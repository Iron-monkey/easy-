<view class="count">{{doneNum}}/{{allNum}}</view>
<view class="formula">
  {{formula}} =
  <input type="number" class="test" bindinput="saveReasult" focus="{{focus}}" value="{{content}}" bindchange="change"/>
</view>
<view class="next" bindtap="nextFormula" >{{viewText}}</view>